[project]
name = "movie-recommender"
version = "0.1.0"
description = "A hybrid movie recommendation system using content-based and collaborative filtering"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
